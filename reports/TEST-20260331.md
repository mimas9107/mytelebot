# Test Report 2026-03-31

## Scope
- Validate new Python mock target server syntax.
- Validate Next.js production build after webhook error-classification changes.
- Validate local mock target health and command endpoints.
- Validate the new `/admin/audit` route builds successfully.
- Validate device alias support, migration, and admin registry edits.
- Validate provider / target test connection UI compiles under version `0.2.0`.
- Validate SQLite backup admin page compiles under version `0.3.0`.
- Validate Telegram update dedupe migration and webhook guard under version `0.4.0`.
- Validate webhook smoke script and response contract docs under version `0.5.0`.
- Validate provider edit flow, command alias migration, and command edit flow under version `0.6.0`.
- Validate shared `reasonCode` taxonomy in webhook JSON and Telegram replies under version `0.7.0`.
- Validate target edit flow under version `0.8.0`.
- Validate command dry-run flow under version `0.9.0`.
- Validate expanded command argument validation under version `0.9.1`.
- Validate hmac auth, confirmation flow, and command cooldown under version `0.9.2`.
- Validate health endpoints under version `0.9.3`.
- Validate SQLite restore safeguards, provider JSON strictness options, and fixture-based webhook integration tests under version `0.9.4`.
- Validate Telegram allowlist-to-admin linking UI under version `0.9.5`.
- Validate provider API key rotation flow build and admin surface under version `0.9.6`.
- Validate richer Telegram replies, stronger webhook error classification, and audit shortcut update under version `0.9.7`.
- Validate structured operational metrics and system event summary under version `0.9.8`.
- Validate system timestamp card sizing tweak and PostgreSQL migration-path docs under version `0.9.9`.

## Commands
```bash
python3 -m py_compile scripts/mock_device_server.py
python3 -m py_compile scripts/mock_llm_server.py
python3 -m py_compile scripts/test_webhook_integration.py
```

Result:
- Pass

```bash
export NVM_DIR="$HOME/.nvm"
. "$NVM_DIR/nvm.sh"
nvm use "$NODE_VERSION" >/dev/null
npm run build
```

Result:
- Pass
- Next.js build completed successfully for all current routes.
- Build includes dynamic route `/admin/audit`.
- Build includes device alias support and inline device edit flow.
- Build includes provider and target connection test actions.
- Build includes SQLite backup page and server actions.
- Build includes Telegram update dedupe schema and webhook guard.
- Build includes webhook smoke script references and response contract documentation.
- Build includes provider edit flow and command alias support.
- Build includes shared `reasonCode` response fields for webhook flow.
- Build includes target edit flow in the registry admin page.
- Build includes command dry-run UI and request rendering.
- Build includes expanded command argument validation and webhook reason handling.
- Build includes hmac auth support, confirmation persistence, and command cooldown logic.
- Build includes `/api/health`, `/api/health/db`, and `/api/health/targets`.
- Build includes SQLite restore UI and provider capability / strict JSON controls.
- Build includes Telegram allowlist link/edit flow for active admin users.
- Build includes dedicated provider API key rotation flow and secret history display.
- Build includes stronger provider/dispatch error taxonomy and updated `/admin/audit` shortcut placement.
- Build includes `/api/metrics` and operational metrics/events on `/admin/system`.

```bash
curl -s "$APP_URL/api/metrics"
```

Result:
- Pass
- Metrics endpoint returned structured counts and recent operational events JSON.
- Build includes updated `/admin/system` metric card sizing and new migration-path documentation.

```bash
export NVM_DIR="$HOME/.nvm"
. "$NVM_DIR/nvm.sh"
nvm use "$NODE_VERSION" >/dev/null
export TELEGRAM_WEBHOOK_SECRET='...'
npm run test:integration:webhook
```

Result:
- Pass
- Fixture-based integration test verified:
  - dispatch success for explicit aliased light command
  - validation failure with `device_not_found`
  - duplicate `update_id` dedupe handling
 - Local health route responds during integration run.

```bash
export TELEGRAM_WEBHOOK_SECRET='test-secret'
UPDATE_ID=2001 bash scripts/test_webhook.sh "把light_01打開"
```

Result:
- Script renders a valid webhook payload and prints the chosen `update_id`.
- Network dispatch was not executed in the sandbox because no local app server was started in this test run.

```bash
python3 scripts/mock_device_server.py
curl -s -H 'Authorization: Bearer dev-secret' http://127.0.0.1:8000/health
curl -s -X POST http://127.0.0.1:8000/device/light_01 \
  -H 'Authorization: Bearer dev-secret' \
  -H 'Content-Type: application/json' \
  -d '{"state":"ON"}'
```

Result:
- Pass
- `/health` returned service metadata.
- `/device/light_01` accepted a dispatcher-style JSON payload and returned updated device state.

## Environment Notes
- `docs/testing-standard.md` now exists and defines the current local testing baseline.
- Local socket binding required elevated execution outside the default sandbox.

## Conclusion
- Code changes compile successfully.
- Mock target server script is syntactically valid.
- Mock target server responds correctly for health and device command requests.
