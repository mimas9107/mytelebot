services:
  - type: web
    name: mytelebot-mock-target
    runtime: python
    rootDir: .
    buildCommand: pip3 install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
